  gem install sinatra
  gem install datamapper
  gem install haml
  gem install sqlite3
  gem install jdbc-sqlite3
  gem install do_sqlite3
  gem install dm-jdbc-adapter
  gem install dm-do-adapter
  gem install dm-sqlite-adapter
